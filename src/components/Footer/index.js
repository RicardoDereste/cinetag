import styles from './Footer.module.css';

function Footer() {
    return (
        <footer className={styles.footer}>
            <h2>Developed by Ricardo</h2>
        </footer>
    )
}

export default Footer;